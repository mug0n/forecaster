# forecaster
A weather forecast addon for Ashita v4.

The data was pulled from the LandSandBoat project but should be compatible with
the older versions of the project, namely Darkstar Project and Project Topaz.

### Usage
/forecast <Auto-Translate Zone Name|Zone ID> <Forecast Length>
Default values will be assumed if omitted.